// CRT dressing. Scanlines REMOVED (founder, 2026-06-12 — same call as the
// video renders' bare mode). Only the soft edge vignette remains.
export function Scanlines() {
  return <div className="vignette" />;
}
